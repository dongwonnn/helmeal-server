let mongoose;_e65‍.x([["default",()=>_e65‍.o]]);_e65‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;

// Meal 스키마 정의
const MealSchema = new Schema({
  id: String,
  title: String,
  protein: String,
  carb: Number,
  fat: Number,
  cal: Number,
  integrity: String,
  proteinImgUrl: String,
  largeImgUrl: String,
  smallImgUrl: String,
});

// 모델 인스턴스 생성 (스키마 이름, 스키마 객체)
const Meal = mongoose.model('Meal', MealSchema);
_e65‍.d(Meal);
