let Koa,Router,bodyParser,mongoose,api;_957‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_957‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_957‍.w("koa-bodyParser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_957‍.w("mongoose;",[["default",["mongoose"],function(v){mongoose=v}]]);_957‍.w("./api",[["default",["api"],function(v){api=v}]]);require('dotenv').config();






const { PORT, MONGO_URL } = process.env;

mongoose
  .connect(MONGO_URL, { useNewUrlParser: true, useFindAndModify: false })
  .then(() => {
    console.log('connect to MongonDB');
  })
  .catch((e) => {
    _957‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes());

// 라우터 적용 전 bodyParser 적용
app.use(bodyParser());

app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
  _957‍.g.console.log(`Listening to port %d`, port);
});
