let Koa,Router,bodyParser,mongoose,api;_b32‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_b32‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_b32‍.w("koa-bodyParser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_b32‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_b32‍.w("./api",[["default",["api"],function(v){api=v}]]);require('dotenv').config();






const { PORT, MONGO_URI } = process.env;

_b32‍.g.console.log(MONGO_URI);

mongoose
  .connect(MONGO_URI, {
    useNewUrlParser: true,
    useFindAndModify: false,
    useUnifiedTopology: true,
  })
  .then(() => {
    console.log('connected to MongoDB');
  })
  .catch((e) => {
    _b32‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes());

// 라우터 적용 전 bodyParser 적용
app.use(bodyParser());

app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
  _b32‍.g.console.log(`Listening to port %d`, port);
});
