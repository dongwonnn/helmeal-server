let Koa,cors,Router,bodyParser,mongoose,api,jwtMiddleware;_899‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_899‍.w("@koa/cors",[["default",["cors"],function(v){cors=v}]]);_899‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_899‍.w("koa-bodyParser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_899‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_899‍.w("./api",[["default",["api"],function(v){api=v}]]);_899‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();









const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI, {
    useNewUrlParser: true,
    useFindAndModify: false,
    useUnifiedTopology: true,
  })
  .then(() => {
    console.log('connected to MongoDB');
  })
  .catch((e) => {
    _899‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes());

// 미들웨어 적용
app.use(cors());
app.use(bodyParser());
app.use(jwtMiddleware);

app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
  _899‍.g.console.log(`Listening to port %d`, port);
});
