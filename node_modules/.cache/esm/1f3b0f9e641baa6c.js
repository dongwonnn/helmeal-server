let mongoose;_641‍.x([["default",()=>_641‍.o]]);_641‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;

// 스키마 정의
const PostSchema = new Schema({
  title: String,
  body: String,
  tages: [String],
  publishedDate: {
    type: Date,
    default: Date.now,
  },
});

// 모델 인스턴스 생성 (스키마 이름, 스키마 객체)
const Post = mongoose.model('Post', PostSchema);
_641‍.d(Post);
