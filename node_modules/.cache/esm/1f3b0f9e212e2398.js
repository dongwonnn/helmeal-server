let mongoose;_212‍.x([["default",()=>_212‍.o]]);_212‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;

// 스키마 정의
const PostSchema = new Schema({
  title: String,
  body: String,
  tags: [String],
  publishedDate: {
    type: Date,
    default: Date.now,
  },
  user: {
    _id: mongoose.Types.ObjectId,
    username: String,
  },
});

// 모델 인스턴스 생성 (스키마 이름, 스키마 객체)
const Post = mongoose.model('Post', PostSchema);
_212‍.d(Post);
